@startuml
start

if (Желаете умный дом) then (Да)
else (нет)
   :Делаем древние выключатели;
   kill
endif

if (какой бюджет?) then (больше 1 миллиона)
   :Устанавливаем систему на шине KNX;
    if (Полная автоматизация?) then(Больше 2х миллионов)
        :Ставим ABB и Schneider;
    else(Не более 2х миллионов)
        :Ставим HDL\n С возможностью улучшения до любого бренда;
    endif
else(Меньше миллиона)
:Ставим HDL на шине BusPro;
  if(Хотя бы с телефона нужно управлять) then (Да)
  :Ставим Iridium;
  else
  endif
endif
:Вы продали умный дом;
kill
@enduml
